apiVersion: 1

providers:
  - name: V-Scan Dashboards
    orgId: 1
    folder: V-Scan
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
