apiVersion: 1

datasources:
  - name: V-Scan SQLite
    type: frser-sqlite-datasource
    access: proxy
    isDefault: true
    jsonData:
      path: /app/data/vscan.db
    version: 1
    editable: false
